# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m certviewer --help
```

## Configure

Create a config file (JSON or YAML) and point the tool at it:

```bash
python -m certviewer run --config config.json
```